name := "fork-join-lab"

version := "0.1"

scalaVersion := "2.13.11"

idePackagePrefix := Some("de.othr.dco")

/*
libraryDependencies += (
  "mavenLikeGroupId" % "mavenLikeArtifactId" % "version" // for "external" dependendies
  "mavenLikeGroupId" %% "mavenLikeArtifactId" % "version" // for "Scala-related" dependendies (deals with naming conventions of libraries for Scala versions >= 2.13)
)
*/
